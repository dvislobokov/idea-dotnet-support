// Does not compile on purpose, and is NOT a part of the solution (it would break Build Solution).
// Make a ".NET Project" run configuration for Broken.csproj by hand and press Debug:
// the errors below have to show up in the Build tool window and in the editor, and no debug session must stay open.
int count = "three";          // CS0029
Console.WriteLine(missing);   // CS0103
